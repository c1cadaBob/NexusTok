# File path: /etc/systemd/system/nexustok.service
# sudo systemctl daemon-reload
# sudo systemctl start nexustok
# sudo systemctl enable nexustok
# sudo systemctl status nexustok
[Unit]
Description=NexusTok Service
After=network.target

[Service]
User=ubuntu  # 注意修改用户名
WorkingDirectory=/path/to/nexustok  # 注意修改路径
ExecStart=/path/to/nexustok/nexustok --port 3000 --log-dir /path/to/nexustok/logs  # 注意修改路径和端口号
Restart=always
RestartSec=5

[Install]
WantedBy=multi-user.target
